package gym.management.Sessions;

/**
 * The ForumType enum is a simple enumeration that defines different types of forums. These types can represent categories based on gender and age, which could be used in a system to categorize people or groups for different forums or discussions.
 */
public enum ForumType {
    Female, Seniors, Male, All
}
